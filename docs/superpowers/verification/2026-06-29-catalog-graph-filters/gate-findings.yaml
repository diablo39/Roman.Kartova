# gate-findings.yaml — per-gate findings telemetry for one slice.
# dod.md records each gate's PASS/FAIL/PENDING/N-A status; this file records what it FOUND.
# A gate with status PASS and no entries here ⇒ it ran and found nothing.

slice:  2026-06-29-catalog-graph-filters
branch: feat/catalog-graph-filters
head:   1d24ba2

# gate slugs: build | reviews | suite | container | simplify | mutation
#             requesting-review | review-pr | deep-review | manual | terminal-reverify | ci-mirror
# severity: blocking | should-fix | nit
# verdict:  real | delusion
findings:
  - gate: reviews
    severity: nit
    verdict: real
    title: "Task 5: missing EOF newline in multi-select.test.tsx"
    note: "fixed in 43fc997 (amended)"
  - gate: reviews
    severity: nit
    verdict: real
    title: "Task 5: new Set(selectedKeys) allocates per render in MultiSelect"
    note: "not fixed — negligible for small option lists; useMemo only if profiling warrants"
  - gate: reviews
    severity: nit
    verdict: delusion
    title: "Task 5: uncontrolled MultiSelect tests 'deleted'"
    note: "file was new this task; brief scoped it to the 2 controlled cases. FilterBar regression (16/16) covers the uncontrolled/FormData path"
  - gate: reviews
    severity: nit
    verdict: real
    title: "Task 6: missing EOF newline in EntityGraphNode.tsx"
    note: "fixed in 79f0517 (amended)"
  - gate: reviews
    severity: nit
    verdict: real
    title: "Task 6: dimmed? comment dropped the 'focus never dims' callsite note"
    note: "restored fuller comment in 79f0517 (amended)"
  - gate: reviews
    severity: nit
    verdict: real
    title: "Task 7: name prop on controlled MultiSelect renders inert hidden inputs"
    note: "not fixed — name is a required MultiSelect prop; component is a canvas overlay (no enclosing form) so hidden inputs are inert. Intentional."
  - gate: reviews
    severity: nit
    verdict: real
    title: "Task 8: React import placed after vi.mock block in page test"
    note: "deferred to final-review triage — harmless (Vitest hoists vi.mock; factory closes over React); organizational only"
  - gate: reviews
    severity: nit
    verdict: real
    title: "Task 8: 'as string[]' casts + wrong 'GraphFilters not exported' comment in page-test mock"
    note: "deferred to final-review triage — cosmetic; GraphFilters IS exported (graphFilter.ts:4). Harmless at runtime, false comment trail"
