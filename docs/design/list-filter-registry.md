# List-Filter Registry

**Status:** Living document
**Owner:** Roman Głogowski (solo developer)
**Governing decision:** [ADR-0107](../architecture/decisions/ADR-0107-list-filtering-consideration-and-filterbar-ui.md) (consideration mandate + `<FilterBar>` UI), [ADR-0095](../architecture/decisions/ADR-0095-cursor-pagination-contract.md) (`f` filter-map wire format).

## Purpose

This is the **canonical, per-list record of the filter decision** required by ADR-0107 clause 1. Every list screen appears here exactly once. Each list slice's design carries a "Filter Proposal" section; its outcome is mirrored into the row below, so anyone scanning this file can confirm filtering was considered for every list — and what was decided.

**Status legend:**

- **built** — filters shipped via `<FilterBar>`/`useListFilters`.
- **built (pre-standard)** — filters shipped before ADR-0107; refactor to `<FilterBar>`/`useListFilters` is a tracked candidate, not necessarily same-slice.
- **deferred** — considered, consciously not built yet; the deferral target is named.
- **none-needed** — bounded/short list where filtering adds no value.
- **pending** — list exists but its filter decision has not been recorded; resolve at its next slice.

**Control availability:** text search + boolean toggle + **single-select** + **multi-select** controls are **built** (available for new filter specs). Date-range remains reserved (ADR-0107 clause 1), to be built when a screen first needs it.

## Registry

| List screen | Route | Filter fields | Status | Owning story | Notes |
|---|---|---|---|---|---|
| Applications | `/catalog` | `displayNameContains` (text) + `lifecycle` (multi-select) + `teamId` (multi-select) + `systemId` (multi-select) (FilterBar) | **built** | E-02.F-01 | Text search + lifecycle + team multi-select via `<FilterBar>`/`useListFilters`; submit-driven + URL-backed (repeated params `?lifecycle=&teamId=`). **Lifecycle multi-select replaces the `includeDecommissioned` boolean** (none-selected ⇒ ADR-0073 default-hide of Decommissioned; selecting Decommissioned reveals them). Team multi-select sources the first 200 teams from `useTeamsList`. Lifecycle + team **sort: opt-out** (explicit — enum-ordinal/needs-join, low value). Pulled the lifecycle/team facets forward from E-05 (faceted search). created-by facet still deferred → E-05. Field `successorApplicationId`: column=no / sort=no / filter=no — deprecation migration guidance surfaced on detail page only; not a list dimension (ADR-0107 field-addition trigger; ADR-0110 App→App successor). Field `system` (2026-07-30 field-addition trigger, catalog-system-membership design §4): column ✓ (`System`, link, "—" when unassigned) / sort ✗ (derived over a `PartOf` edge join, no index — deliberate, not an oversight) / filter ✓ (multi-select `systemId`) — **shipped in sub-slice A2** (A1 only made membership writable; A2 wires the column and filter). `systemId` values are deliberately **unvalidated** — an unknown or cross-tenant id matches nothing rather than erroring (validation can't distinguish "unknown" from "exists in another tenant," so it would add a response branch without adding a control) — and the value list is de-duped then capped at 50 distinct ids (400 `too-many-filter-values` beyond that). **2026-08-05 (gate-2 follow-up): the same 50-value cap now covers `teamId` and `lifecycle` here** — every multi-select whose values are encoded into the cursor `f`-map is capped, since an uncapped list breaks page-2 replay regardless of which field it is. The `lifecycle` cap is defence-in-depth only (3 enum members can never reach 50). |
| Services | `/catalog/services` | `displayNameContains` (text) + `teamId` (multi-select) + `health` (multi-select) + `systemId` (multi-select) (FilterBar) | **built** | E-02.F-02 | Text search + team + health + system multi-select via `<FilterBar>`/`useListFilters`; createdBy facet still deferred → E-05. Field `system` (2026-07-30 field-addition trigger, catalog-system-membership design §4): column ✓ (`System`, link, "—" when unassigned) / sort ✗ (derived over a `PartOf` edge join, no index — deliberate, not an oversight) / filter ✓ (multi-select `systemId`) — **shipped in sub-slice A2** (A1 only made membership writable; A2 wires the column and filter). `systemId` values are deliberately **unvalidated** — an unknown or cross-tenant id matches nothing rather than erroring — and the value list is de-duped then capped at 50 distinct ids (400 `too-many-filter-values` beyond that). **2026-08-05 (gate-2 follow-up): the same 50-value cap now covers `teamId` and `health` here**, for the same cursor-`f`-map reason; the `health` cap is defence-in-depth only (4 enum members can never reach 50). |
| APIs | `/catalog/apis` | `displayNameContains` (text) + `style` (multi-select) + `teamId` (multi-select) (FilterBar) | **built** | E-02.F-03 (FU-9) | Text search + style + team multi-select via `<FilterBar>`/`useListFilters`. Sort allowlist `{displayName, style, version, createdAt}`, default `displayName asc`. Backend `ListApis` filter params mirror `ListServices`. Shipped 2026-07-04, promoted from the FU-9 deferral recorded at the API-entity slice (S-01, 2026-07-03). `hasSpec` (2026-07-07, field-addition trigger): column ✓ (Spec indicator), sort ✗ (presence not meaningful sort key), filter deferred (has-spec/no-spec filter is follow-up). Field `system` (2026-07-30 field-addition trigger, catalog-system-membership design §4): **N/A** — an `Api` cannot be `PartOf` a System (`RelationshipTypeRules` disallows the pair), so this list has no System dimension to add. **2026-08-05 (gate-2 follow-up): `teamId` is now de-duped and capped at 50 distinct ids (400 `too-many-filter-values`)** — `ListApisHandler` encodes it into the cursor `f`-map, so it carried the same page-2 break as the Applications/Services lists. |
| Teams | `/teams` | `displayName` text search | **built** | E-03.F-02 | Renders via the shared `<FilterBar>` + `useListFilters`; default sort **`displayName asc`**. `<FilterBar>` shell is a collapsible disclosure panel (expanded by default), standard across all consumers. First consumer of the ADR-0107 surface (slice 2026-06-21). |
| Members / Users | `/members` (`GET /users`) | `role` (single-select) + `q` name/email search (FilterBar) | **built** | E-03.F-01.S-05 | Role single-select + name/email text search via `<FilterBar>`/`useListFilters`; submit-driven + URL-backed (`?role=&q=`). |
| Entity Relationships | App/Service detail → Relationships section (`GET /catalog/relationships?entityKind=&entityId=`) | none (no `<FilterBar>` facets) | **deferred** | E-04.F-01/F-02.S-02 | Per-entity embedded list (backend Slice 1a 2026-06-24; UI Slice 1b). `direction` (outgoing/incoming/all) is a **core query param**, not a facet. Facets considered + all deferred **as `<FilterBar>` facets**: `origin` (only `manual` exists → E-04.F-01.S-03/04 when scan/agent land), `type`, related-entity-`kind` (only 2 kinds) → all → E-05 faceted search. **`type` backend param BUILT 2026-07-30 (E-03.F-03.S-01 A1)** — optional, single-valued `?type=<RelationshipType>`, applied **before** pagination and encoded into the ADR-0095 cursor `expectedFilters` fingerprint (as is `excludeApiEdges`, fixed in the same slice); consumed today by the component System-membership read (`type=partOf&limit=1`) and the System Members list. No UI facet yet — the facet itself stays deferred to E-05, so this row is *param built, facet deferred*, not "deferred" outright. **Sort:** `{ CreatedAt (default desc), Type }`; sort-by-related-name opt-out (cross-table keyset, deferred). Default deviates from `displayName asc` — relationship rows have no own displayName. |
| Dependency-graph filters | `/graph` | `kind` (multi-select, four options: Application/Service/API/System) + `teamId` (multi-select) | **built** | E-04.F-02.S-05 | Canvas-overlay surface (React Flow `<Panel>`, ADR-0040) — **client-side dim/fade** of non-matching nodes (focus never dims; an edge dims iff either endpoint dims), **live-apply** (no submit), state in `sessionStorage` keyed by focus (only `?focus` in URL). Reuses the controlled `MultiSelect` + `useTeamsList`, **not** the `<FilterBar>` chrome (ADR-0040). Filter Proposal outcomes — `kind`: built · `teamId`: built · **status** (Lifecycle/Health): deferred (needs `GraphNodeDto` enrichment + combined-status story) · **origin**: deferred (only `Manual` exists → Phase 2 scan/agent / E-06) · **domain** + **criticality**: deferred (no backing field → new-field epic). Slice 2026-06-29. **2026-08-06 (catalog-system-graph-nodes, FU-A): `kind`'s value set widened from `RelationshipKind` to `EntityKind` — `KIND_OPTIONS` (`GraphFilterControls.tsx`) gained a fourth option, `{ label: "System", value: "system" }` — no change to the `teamId` facet or the built/deferred outcomes above.** |
| API surface panel | Service/Application detail → API Surface section (`GET /catalog/api-surface?entityKind=&entityId=`) | none (no `<FilterBar>` facets) | **none-needed** | E-02.F-03.S-03 | Bounded embedded panel (`ApiSurfaceSection`), not a top-level list — no `<FilterBar>`. Default sort: **sync-styles-first, then `displayName asc`** — client-side, non-configurable (no `sortBy`/`sortOrder` query params). All filter facets (origin Direct/Derived, style, team) — **none-needed**: bounded embedded panel, small N (a component's direct+derived API edges). Slice 2026-07-08 (`feat/catalog-unified-api-view`), sub-slice A of S-03; sub-slice B (derived service↔service `depends-on` in `/graph`) is a follow-up, not this panel. |
| Derived dependencies panel | Service detail → Derived dependencies section (`GET /catalog/derived-dependencies?entityId=`) | none (no `<FilterBar>` facets) | **none-needed** | E-02.F-03.S-03 (FU-B, B2) | Bounded read-only embedded panel (`DerivedDependenciesSection`), service-only, not a top-level list — no `<FilterBar>`. Default sort: **client-side `displayName asc`**, non-configurable (no `sortBy`/`sortOrder`). All facets (provenance API/app, team) — **none-needed**: bounded, small N (a service's derived neighbours). Also surfaced as dashed edges in the per-service mini-graph. Slice 2026-07-09 (`feat/catalog-derived-dependencies-b2`, B2). |
| Systems | `/catalog/systems` (list screen + `<FilterBar>`) | `displayNameContains` (text) + `teamId` (multi-select) | **built** | E-03.F-03.S-01 | Sort allowlist `{displayName, createdAt}`, default `displayName asc` (standard). Filters mirror the `ListApis` shape minus `style` (Systems have no style dimension). List screen wires `useListUrlState`/`useListFilters`/`<FilterBar>` + `<DataTable>` (slice 2026-07-22). Renders `displayNameContains` text + `teamId` multi-select. Deferred: `createdAt` range (reserved control, ADR-0107 clause 1, no screen needs it yet) and `memberCount` (needs a `PartOf`-edge aggregate join — deferred alongside the browse-by-hierarchy story, E-03.F-03.S-02; revisited and reconfirmed deferred at the 2026-07-30 field-addition trigger, catalog-system-membership design §4 — still needs a backend aggregate). System resolution stays derived over `PartOf` edges — no denormalized column, no projection. Revisit a read model (a `v_component_system` view over the edges as one source of truth for what is now five resolution sites, no dual write) when either `memberCount` or the S-02 hierarchy browse lands, since both need an aggregate over the same edges. **2026-08-05 (gate-2 follow-up): `teamId` is now de-duped and capped at 50 distinct ids (400 `too-many-filter-values`)** — `ListSystemsHandler` encodes it into the cursor `f`-map, so it carried the same page-2 break as the other list endpoints. |
| Infrastructure | `/catalog/infrastructure` (`GET /catalog/infrastructure`) | `teamId` (multi-select) + `type` (multi-select) (FilterBar) | **built** | Infrastructure/VM slice 1 (Task 11) | Generic list across every `InfrastructureType` — shared columns only, never deserializes the opaque `Attributes` jsonb payload (ADR-0111 amendment). Renders as `AllInfrastructureListPage`, wired via `useListUrlState`/`useListFilters`/`<FilterBar>` + `<DataTable>`; live `<FilterBar>` facet is `type` (multi-select). Sort allowlist `{displayName, createdAt, type}` (FW3: `type` added — it's a typed smallint column, not JSONB, so outside the JSONB-sort deferral below), default `displayName asc` (standard, mirrors Services/APIs/Systems). `type` filter is a multi-select case-insensitive enum-name filter (today only `virtualMachine`); numeric/unknown tokens rejected 400 `invalid-type-filter` (mirrors `health`/`style`). `teamId` de-duped + capped at 50 distinct ids (400 `too-many-filter-values`), same as every other multi-select id filter. **JSONB-attribute sort (e.g. by a type-specific attribute) is deferred to a follow-up** — this slice-1 allowlist is shared-columns only. |
| VMs (Virtual Machines) | `/catalog/infrastructure/vms` (`GET /catalog/infrastructure/vms`) | `teamId` (multi-select) + `powerState` (single-select) + `os`/`region`/`hostname`/`ipAddress` (text, exact jsonb-containment match) (FilterBar) | **built** | Infrastructure/VM slice 1 (Task 11) | Type-specific VM surface — `Type` is implicitly fixed to `VirtualMachine` by the handler, not a query param. Renders as `VirtualMachinesListPage` (+ `VmDetailPage`), wired via `useListUrlState`/`useListFilters`/`<FilterBar>` + `<VmTable>`. `powerState`/`os`/`region`/`hostname` translate to Postgres jsonb object-containment (`@>`, GIN-indexed) via `EF.Functions.JsonContains`; `ipAddress` is array-containment against the `ipAddresses` jsonb array. All five are raw single-value **exact**-equality filters over `VmAttributes` (no substring/partial match, no enum parse unlike `type`/`health`/`style`) — blank/whitespace ⇒ no predicate; FW3 fixed the FE text-filter placeholders (were "Search by…", implying substring match; now read "Exact …"/"IP address…") to match this exact-match behavior. Sort allowlist `{displayName, createdAt}`, default `displayName asc` (standard). **JSONB-column sort (e.g. by `powerState`/`os`/`region`) is deferred to a follow-up** — slice-1 allowlist is shared-columns only, mirrors the `Infrastructure` row above. `teamId` de-duped + capped at 50 distinct ids (400 `too-many-filter-values`). |

## Planned filtering surfaces (not yet built)

These backlog stories define multi-attribute filtering that, when built, MUST use `<FilterBar>` (ADR-0107):

- **Tag filtering across catalog** — multi-tag, AND/OR, URL-shareable, live (E-03.F-04.S-03).
- **Faceted search** — multi-select by entity type / team / tags / owner, live counts (E-05.F-01.S-02).
- **Repo-import filters** — name / language / activity (E-08.F-02.S-02).
- **Dashboards** — status board, environment map, maturity/risk heatmaps (E-06, E-17, E-18, E-20).

## How to update

When a list slice is designed: add or update its row from the slice's Filter Proposal outcome. When filters are refactored onto `<FilterBar>`, drop the "(pre-standard)" qualifier.

**New-field check (field-addition trigger, ADR-0107 clause 1).** When a slice adds a new queryable / user-facing field to an entity that already has a list here, revisit that list across all three surface axes — **column? · sort? · filter?** — and update its row (e.g. note "field `criticality`: column=yes / sort=no / filter=deferred→E-05"). New fields default to "reconsider"; opting an axis out is the explicit, recorded decision. This is a design/review gate, not an automated check.
